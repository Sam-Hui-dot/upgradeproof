services:
  app:
    image: upgradeproof-fixture-broken:${APP_TAG}
    build:
      context: ./app
      args:
        BROKEN: "1"
    ports:
      - "18082:8080"
    volumes:
      - app-data:/data

volumes:
  app-data:
