services:
  app:
    image: upgradeproof-fixture-file:${APP_TAG}
    build:
      context: ./app
      args:
        APP_VERSION: target
    ports:
      - "18081:8080"
    volumes:
      - app-data:/data

volumes:
  app-data:
